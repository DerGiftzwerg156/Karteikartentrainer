# CREATE TABLE IF NOT EXISTS standard_card
# (
#     standard_card_id INTEGER        NOT NULL AUTO_INCREMENT,
#     question         varchar(1000) NOT NULL,
#     answer           varchar(1000) NOT NULL,
#     PRIMARY KEY (standard_card_id)
# )